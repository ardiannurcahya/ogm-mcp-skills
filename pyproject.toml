[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "ogm-mcp-skills"
version = "0.2.3"
description = "MCP Server and AI Agent Skills for OpenGraphMemory"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Ardian Nurcahya" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = [
  "httpx>=0.27,<1",
  "mcp>=1.0,<2",
  "python-dotenv>=1,<2",
]

[project.urls]
Homepage = "https://github.com/ardiannurcahya/ogm-mcp-skills"
Repository = "https://github.com/ardiannurcahya/ogm-mcp-skills"
Issues = "https://github.com/ardiannurcahya/ogm-mcp-skills/issues"

[project.scripts]
ogm-mcp-skills = "ogm_mcp_skills.mcp_server:main"
ogm-mcp = "ogm_mcp_skills.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ogm_mcp_skills"]

[dependency-groups]
dev = [
  "hatchling==1.27.0",
  "mypy>=1.10,<2",
  "pytest>=8,<9",
  "pytest-asyncio>=0.23,<1",
  "ruff>=0.5,<1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 88
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
mypy_path = "src"
files = ["src"]
